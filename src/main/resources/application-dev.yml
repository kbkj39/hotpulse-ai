spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/hotpulse
    username: hotpulse
    password: hotpulse123

  data:
    redis:
      host: localhost
      port: 6379

  rabbitmq:
    host: localhost
    port: 5672
    username: hotpulse
    password: hotpulse123

  jpa:
    show-sql: true

iwencai:
  base-url: https://openapi.iwencai.com
  api-key: ${IWENCAI_API_KEY:}

logging:
  level:
    com.hotpulse: DEBUG
    org.springframework.ai: DEBUG
